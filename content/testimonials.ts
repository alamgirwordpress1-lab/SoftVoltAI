import type { Testimonial } from "@/lib/cms/types";

/**
 * TODO(owner): add real, permissioned quotes only — the partner's own words,
 * their name, role, agency and country, plus what we delivered. Never publish
 * a quote you cannot show in writing, and never a stock photo of a stranger.
 * While this list is empty the section renders the honest "first partner" panel.
 */
export const testimonials: Testimonial[] = [];
